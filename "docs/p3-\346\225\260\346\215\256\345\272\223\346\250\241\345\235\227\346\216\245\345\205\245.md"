# 数据库模块接入

数据库不再单独长一套执行和监控。MySQL 实例登记成 CMDB 对象并挂到服务树叶子，变更走工单审批后的剧本，执行仍进任务中心，指标进现有采集池。本轮不接 Kubernetes Operator，也不做独立的数据库页面。

## 实例对象

模型编码是 `db_instance`。`db_instances` 仍是登记表，编号不变。每条实例有一条 `object`，`object_node` 挂在原来的 `tree_node_id` 上。已有实例在启动时补对象，重复启动不会再造一条。

对象上只放稳态配置：主机、端口、版本、角色（主或从）、环境（开发、测试、生产）、连接账号、采集地址、主库指向。口令不入库，复制延迟不入库，运行状态不入库。

登记实例要显式填写 `开发` 或 `测试`。不填时环境是 `生产`。生产实例不能走真实执行：模拟关闭时审批后的变更直接失败，错误是「生产实例不走真实执行」，也不建任务。

列表和详情走原来的 `/api/db/instances`，并带上 `objectId`。列表按服务树收窄。详情对看不见的节点返回「没有这个实例」。新建走 `POST /api/db/instances`，动词 `admin`，资源 `db_instance`。没有节点拒绝。平台管理员可以登记别人负责的叶子，但仍然必须挂在叶子上。备份和还原登记仍用原来的节点运维判断（`tree.CanWrite`，平台管理员不直过），不执行备份程序。这两条后续收口到统一权限。

## 变更

三张工单模板：建库、建账号、SQL 变更。模板本身没有状态。审批通过后只创建剧本 `db.change` 的 Run，不在审批接口里连数据库。其它模板仍按原来的方式，变更申请不启动 Run。

剧本只有一步 `db_exec`。这一步核对工单已经审批（`approved_by` 有值，状态是待执行或已完成，并且工单和实例在同一个叶子），再调用任务中心 `task.Open`。没有审批记录、工单还在待审批，都不下发任务。

动作只有三个：

| 动作 | 下发内容 |
|---|---|
| `create_database` | `CREATE DATABASE` 加一个字母开头的库名。库名 `xuntai` 拒绝 |
| `create_account` | `CREATE USER '账号'@'主机'`。语句里没有 `IDENTIFIED BY`。口令由实例侧人工配置，平台不下发 |
| `sql_change` | 单条 `CREATE TABLE`、`ALTER TABLE` 或 `CREATE INDEX`。`DROP`、`DELETE`、`TRUNCATE`、`INSERT`、`UPDATE`、`GRANT` 以及多条语句都拒绝 |

平台不打开 SQL 连接。语句写进任务脚本，结果写进 `task_job` 的主机结果。

`XUNTAI_TASK_MOCK` 设为 `1`（也接受 `true`、`on`、`yes`）时，任务中心自己把已下发的结果回写成成功或失败，生产不要设置。没有代理、模拟又关闭时，开发或测试实例的变更任务停在「已下发」，一直等代理回写，Run 也停在执行中。全流程演示需开 `XUNTAI_TASK_MOCK`。生产实例在模拟关闭时直接失败，不建任务。失败的结果留在任务上，工单停在待执行。

## 监控

采集任务的发现方式可以是 `db`。拉取配置时，这个叶子上的实例成为目标：有采集地址就用采集地址，否则用主机和任务端口，端口空着则用 `9104`。规则和发送组沿用原来的对象挂载，把 `objectId` 指到实例对象即可。不新建告警通道，也不做看板。

## 后续

Kubernetes 上的 MySQL Operator、独立的库管页面、SQL 审核引擎、连接池，都不在本轮。要做的时候仍从对象和工单出去，不另起一套执行通道。
