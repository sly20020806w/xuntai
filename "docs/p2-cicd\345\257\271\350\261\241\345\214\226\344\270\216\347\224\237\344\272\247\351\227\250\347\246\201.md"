# CICD 对象化与生产门禁

## 对象化方案

发布项仍在 `cicd_deploy_items`。发布单继续用 `item_id` 指向这张表，阶段记录不动。每条发布项增加 `object_id`，对应 `object` 里的一条记录，模型编码是 `release_item`，并在 `object_node` 挂到原来的叶子。

选映射而不是把行搬进对象表：发布单、回滚和开发发布都按发布项编号走，搬走会改这些外键。已有发布项在启动时补对象，编号、仓库、镜像和原来的发布单都还在。补对象是幂等的：发布项已经有 `object_id` 且对象还在时，再次启动只核对叶子关系，不新建对象。节点用发布项上原来的 `tree_node_id`，也就是原来的叶子，不按名称重找。

对象属性保存仓库、镜像、阶段、集群，以及发布策略：`batches`、`strategy`、`verify_url`、`executor`。列表和详情带 `objectId`。可以按 `nodeId`、`objectId` 查。同一条对象会出现在 CMDB 对象列表里，服务树按节点收窄后能看见。

## 生产剧本

`release.prod.single` 和 `release.prod.rollback` 仍是关卡、部署、然后人工等待。部署步在等待之前写镜像。回滚用 `previous_tag`，不另开改版本的接口。

发布项上的批次不足两条时，人工等待仍是原来的一步 `confirm`。两批及以上，启动 Run 时把这一步展开成同名的 `wait_manual`。每一批都是剧本步骤：停在 `paused`，继续要带当前步骤和版本，下一步若还是等待就再次停下。批次和 `verify_url` 只从发布项对象读。Run 入参里的同名字段不生效。

这里的分批是分批人工确认，不是灰度流量发布。镜像仍一次写成完整标签，不按批次切流量或副本。灰度要以后再接 Argo Rollouts。

`GET /api/playbook/runs` 和 `GET /api/playbook/runs/:id` 都返回全部步骤。每个 `wait_manual` 带自己的 `id`（继续时的 `stepId`）、`key`、`kind`、`status`，另外有 `currentStepId` 指向当前步。发布页的「继续」找状态为 `waiting` 的那一步提交，所以不用改前端就能按顺序继续每一批。还没轮到的批次在列表里，但不能跳过当前步去继续它。

`k8s_apply` 仍是生产镜像的唯一写入，一次写成完整标签，不按批次切副本。返回里的执行器是 `platform`。

## 执行器边界

本轮没有接入 Argo Rollouts 或 OpenKruise。`executor` 必须留空，写入组件名会拒绝，错误是「执行器还没有接上」。`strategy` 只记在发布项上，例如 `manual` 或 `canary`，平台不按这个字段去调控制器。灰度流量发布不在本轮，后续要做时再接 Argo Rollouts。

## 权限

资源类型保持 `release_item`。节点取发布项的叶子，也就是对象挂上的那个节点。无节点运维 403，有节点运维通过，平台管理员直过。

开发环境发布仍直接完成并写开发镜像。生产阶段确认仍是 409。同一幂等键的未结束 Run 仍是 409。工单审批只创建 Run。
