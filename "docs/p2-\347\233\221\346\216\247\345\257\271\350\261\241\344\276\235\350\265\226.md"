# 监控对象依赖

告警规则和发送组可以挂在服务树节点上，也可以再挂一个应用或一个 CMDB 对象。对象必须落在这个节点或它的子节点上，否则创建和修改都会拒绝。应用是否落在节点上，看它所属项目的节点。

列表仍先按当前用户能看见的节点收窄，再看对象和应用是否也落在这些节点上。响应里带 `nodeId`、`nodeName`、`objectId`、`objectName`。发送组同样带节点，种子数据里交易发送挂在交易，基础架构发送挂在入口，可观测发送挂在可观测。

告警处理走监控接口，不改 Alertmanager 的投递方式，也不改采集。`cmd/webhook` 仍只保留入口。收到指纹以及节点或对象后，用 `tree.OpsOwners` 从对象所在节点往根找运维负责人，由近到远、同一个人只出现一次。认领、屏蔽、升级写到 `monitor_alert_actions`，可按对象或负责人查询。认领可以指定链上的某个人；屏蔽记最近的负责人；升级记上一级，没有上一级就仍是最近的那位。写操作仍用节点运维判断，平台管理员不绕过这一层。

## 本轮没有做故障自愈

预授权剧本没有接上。告警触发后要启动一次 Run，需要按规则准备剧本入参、用告警 ID 做幂等，并保证这条路径不能改去直接调用有副作用的接口。这块和对象绑定、负责人查找、三种处理加在一起体量相当，留到后续。没有剧本配置的规则现在只告警，不会自动建 Run。

生产镜像写入仍是无条件 409。剧本幂等、工单只启动 Run、资源归属校验都没有改。
