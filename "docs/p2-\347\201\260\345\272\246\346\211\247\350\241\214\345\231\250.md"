# 灰度执行器

`k8s_apply` 按发布项对象上的 `executor` 分派。`platform`（或留空）仍是一次写成完整镜像，后面的分批仍是人工确认，不切流量。`rollouts` 走灰度。其它名字仍是 400「执行器还没有接上」，不会悄悄退回 `platform`。

策略只从发布项读。Run 入参里的 `executor`、`weights`、`strategy` 不生效。

## 发布项上的策略

| 字段 | 含义 |
|---|---|
| `strategy` | `canary` 用百分比序列。`batch` 且没写百分比时，按批次均分到 100 |
| `weights` | 金丝雀百分比，例如 `[10, 50, 100]`。最后一档必须是 100。没写时 canary 默认 10、50、100 |
| `stableSeconds` | 每个阶段让 Rollout 暂停的秒数。0 表示停住等人 |
| `failureThreshold` | 失败几次后判定这个阶段没通过。默认 1 |
| `autoRollback` | 失败后把实例镜像写回这个灰度开始前的旧镜像 |

每个百分比是一条 `k8s_apply` 步骤，步骤名是 `w10` 这种。进入该步后 Run 停在 `paused`。继续仍用原来的 `stepId` 和 `version`。

发给 Rollout 的镜像，从第一步起就是这次发布的目标标签，不是等到 100。目标标签来自 Run 里的 `image_tag`；回滚则来自 `previous_tag`。标签里没有 `/` 也没有 `:` 时，前面加上发布项的镜像名，例如 `image_tag` 为 `1.9.5`、镜像名为 `order-api` 时，CRD 里的 `spec.template.spec.containers[0].image` 是 `order-api:1.9.5`。每一档都带上这个镜像，同时把 `setWeight` 设成当前百分比。

「到 100 才写新标签」只指平台库 `k8s_instances.image`。10、50 这些中间档不改这条数据库记录，实例镜像仍显示旧版本。权重到 100，或回滚步骤 `abort`，才把同一条数据库记录改成目标标签。集群上的 Rollout 对象从第一档起就带着新镜像，本平台不根据数据库记录去改集群里已经跑着的 Pod。

`GET /api/playbook/runs` 和详情里每个步骤都有 `id`、`key`、`status`、`output.weight`。

## 回滚

失败时如果开了 `autoRollback`，同一次继续把镜像写回旧版本，Run 变为失败。没有开时，阶段失败，镜像保持当前值，要回旧标签就再走 `release.prod.rollback`。回滚剧本把 `previous_tag` 一次放回 100%，步骤名是 `abort`。没有别的改版本接口。

## 模拟

`XUNTAI_ROLLOUTS_MOCK=1`（也接受 `true`、`on`、`yes`）时，用内存状态机从 0 走到各百分比，再在 100 停住。默认关闭。生产不要设置。打开时进程日志会写「灰度由本地状态机推进，生产环境不要打开」。

模拟忽略 `stableSeconds`，不会按这个秒数睡觉，也不会等控制器。步骤输出里仍记下 `stable_seconds`，方便核对配置有没有读到。测试在同一次调用里立刻继续下一步，没有 `Sleep`。整段灰度测试就是接口往返的时间，通常一两秒，不跟稳定时长走。

## 真实集群

模拟关闭时，适配层按 Argo Rollouts 的 CRD 调用 `PUT/POST /apis/argoproj.io/v1alpha1/namespaces/default/rollouts/{发布项名}`。请求体里的容器镜像就是上面说的目标标签，`setWeight` 是当前百分比，`pause.duration` 来自 `stableSeconds`（为 0 时暂停对象是空的，表示停住等人）。集群要用发布项所在集群记录里的 kubeconfig。没有 kubeconfig 就失败，不会改走模拟。接口只要返回 2xx 就算这一档下发完成，然后 Run 自己停在 `paused`。

仅下发不观测，健康检查待生产验证。下发之后不回读 Rollout 的 `status`，不看 `progressing` / `healthy`，也不等 `stableSeconds` 走完。这条路径没有在真实集群验收。本机没有集群，测试只检查请求体和「没有 kubeconfig 时失败且不改镜像」。没有引入 Argo 的代码库，也没有 client-go。OpenKruise 和蓝绿不在这里。
